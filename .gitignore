# Dependencies
node_modules/
# Lockfiles aren't committed in this repo (Dockerfiles fall back to `npm install`
# when no lockfile is present). Remove these lines if you want reproducible installs.
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build output
dist/
build/

# Test coverage
coverage/
*.lcov

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Local environment overrides (committed .env files stay; per-dev overrides don't)
.env.local
.env.*.local

# Vite cache
.vite/
